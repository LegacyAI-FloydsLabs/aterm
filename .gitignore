# Dependencies
node_modules
node_modules/
ui/node_modules/

# Build output
dist/
ui/dist/

# Runtime artifacts
.aterm-token
*.db
*.db-wal
*.db-shm
*.db-journal

# Environment
.env
.env.*

# OS
.DS_Store

# Logs
*.log

# Media (delivered artifacts go to Floyd Docs, not the repo)
*.mp3
*.mp4
*.wav

# Test artifacts
test-screenshots/

# Agent scratch
.floyd/
.claude/

# Agent research/docs (session artifacts, not project source)
Floyd Docs/

# Agent-generated review / smoke-test reports. Session artifacts that capture
# live server output — they have leaked the .aterm-token and candid internal
# assessments. Never commit; keep them local only.
*_Review.md
*_SMOKE_TEST.md

# Product/strategy decks & reports — not source; keep out of the public repo
VoidDeck/
_REPORTS/

# Runtime logs dir (token is printed into server logs)
logs/
